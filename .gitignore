# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies

# pubspec.lock はコミットする。
# ライブラリではなくアプリなので、依存の解決結果を固定して
# 「こちらを何も変えていないのに CI が落ちる」を防ぐ。

# IDE
.idea/
.vscode/
*.iml
.DS_Store

# Generated platform dirs (regenerated in CI via `flutter create .`)
# 必要ならローカル開発時にコミットしてもよい
/android/
/ios/
/web/
/linux/
/macos/
/windows/
.metadata

# App Store Connect の秘密情報（絶対にコミットしない）
*.p8
.asc.json
scripts/asc/.asc.json
scripts/asc/config.json
